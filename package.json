{
  "name": "rasa",
  "private": true,
  "scripts": {
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint --ignore-path .gitignore --no-error-on-unmatched-pattern .",
    "recipes:parse": "node scripts/parse-recipes.mjs && oxfmt docs/recipe-collections/resources/recipes.json"
  },
  "devDependencies": {
    "oxfmt": "^0.50.0",
    "oxlint": "^1.65.0"
  }
}
